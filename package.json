{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna:bootstrap": "lerna bootstrap --ci",
    "setup": "npm ci && lerna bootstrap --ci"
  },
  "devDependencies": {
    "@types/node": "^15.12.1",
    "lerna": "^4.0.0",
    "typescript": "^4.3.2"
  }
}
